---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rancher
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`rancher.lab.oscarr.nl`)
      services:
        - name: rancher
          port: 80

{{- if .Values.creativeReset.enabled }}
# The restore itself lives in the creative pod's initContainer, so a daily reset
# is just a pod replacement. Recreate is the chart's strategy, so the old pod is
# gone before the new one restores -- no two writers on the same volume.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: minecraft-creative-reset
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: minecraft-creative-reset
rules:
  - apiGroups: ["apps"]
    resources: ["deployments"]
    resourceNames: [{{ .Values.creativeReset.deployment | quote }}]
    verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: minecraft-creative-reset
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: minecraft-creative-reset
subjects:
  - kind: ServiceAccount
    name: minecraft-creative-reset
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: minecraft-creative-reset
spec:
  schedule: {{ .Values.creativeReset.schedule | quote }}
  timeZone: {{ .Values.creativeReset.timeZone | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        spec:
          serviceAccountName: minecraft-creative-reset
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: kubectl
              image: registry.k8s.io/kubectl:v1.36.3
              command:
                - kubectl
                - rollout
                - restart
                - deployment/{{ .Values.creativeReset.deployment }}
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop:
                    - ALL
              resources:
                requests:
                  cpu: 10m
                  memory: 32Mi
{{- end }}
